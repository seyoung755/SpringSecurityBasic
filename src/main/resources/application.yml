server:
  port: 8080
  servlet:
    context-path: /
    encoding:
      charset: UTF-8
      enabled: true
      force: true

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/security?serverTimezone=Asia/Seoul
    username: root
    password:

# 머스태치 의존성 등록 시 기본적으로 설정된다
#  mvc:
#    view:
#      prefix: /templates/
#      suffix: .mustache

  jpa:
    hibernate:
      ddl-auto: update #create update none
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
    show-sql: true

  devtools:
    restart:
      enabled: false

  security:
    oauth2:
      client:
        registration:
          google:
            client-id: 536297725280-ueuf0emcbtaf6lg52jti5m6a468990kp.apps.googleusercontent.com
            client-secret: u9JXflefUELxXW8_V6rrAwHD
            scope:
              - email
              - profile
          facebook:
            client-id: 1740608256122506
            client-secret: 28d269014df7f986caeb65128efbcfab
            scope:
              - email
              - public_profile

          naver:
            client-id: bdB0Aja9VvGYs5jNm2zb
            client-secret: pUZqZQarOD
            scope:
              - name
              - email
            client-name: Naver
            authorization-grant-type: authorization_code
            redirect-uri: http://localhost:8080/login/oauth2/code/naver # 코드를 받는 주소

          kakao:
            client-id: 29acd9d3b3c72fefd67705ee48732445
            scope:
              - profile_nickname
              - account_email
            client-name : Kakao
            authorization-grant-type: authorization_code
            redirect-uri: http://localhost:8080/login/oauth2/code/kakao

        provider:
          naver:
            authorization-uri: https://nid.naver.com/oauth2.0/authorize # /oauth2/authorization/{client_id} 로 접속 시 연결
            token-uri: https://nid.naver.com/oauth2.0/token
            user-info-uri: https://openapi.naver.com/v1/nid/me
            user-name-attribute: response # 회원정보를 json으로 받는데 response라는 key 값을 가짐.

          kakao:
            authorization-uri: https://kauth.kakao.com/oauth/authorize
            token-uri: https://kauth.kakao.com/oauth/token
            user-info-uri: https://kapi.kakao.com/v2/user/me
            user-name-attribute: properties